import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'RegistrAPP',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  },
  plugins: {
    Storage: {
      allowedSchemes: ['app', 'file', 'http', 'https']
    }
  }
};

export default config;
